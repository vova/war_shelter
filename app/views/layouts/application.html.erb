<!DOCTYPE html>
<html>
  <head>
    <title>WarShelter</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'https://cdn.datatables.net/1.11.5/css/dataTables.bootstrap5.min.css' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div class="header bg-info">
      <div class="container">
        <div class="header-logo">
          <p>Our Logo</p>
        </div>
        <div class="header-nav">
          <% if current_user %>
            <%= "@#{current_user.name}" %>
            <%= link_to "logout", destroy_user_session_path, :method => :delete, class: 'btn btn-outline-primary' %>
          <% end %>
          <% if notice %>
            <p class="alert alert-success"><%= notice %></p>
          <% end %>
          <% if alert %>
            <p class="alert alert-danger"><%= alert %></p>
          <% end %>
        </div>
      </div>
    </div>
    <div class="container">
      <div class="row">
        <div class="col-lg-12">
          <%= yield %>
        </div>
      </div>
    </div>
    <footer class="footer-07">
      <div class="container">
        <div class="row justify-content-center">
          <div class="col-md-12 text-center">
            <h2 class="footer-heading"><a href="#" class="logo">Warshelter.com</a></h2>
          </div>
        </div>
        <div class="row mt-5">
          <div class="col-md-12 text-center">
            <p class="copyright">Copyright © 2022 All rights reserved</a>
            </p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
